# 系统源码交付说明

本目录包含后端、前端、四模型算法、配置、训练评测脚本、自动化测试、正式模型、实验结果和三张检测样本。

## 运行步骤

```powershell
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
```

安装完成后双击“启动系统.bat”，访问 http://127.0.0.1:8000 。前端生产文件已经构建完成，系统会直接使用artifacts/models中的12套模型。

## 验证步骤

```powershell
.venv\Scripts\python.exe -m pytest -q
.venv\Scripts\python.exe scripts\smoke_test_system.py
```

完整MVTec AD数据未重复放入本目录，可运行scripts/download_mvtec_subset.py下载。sample_data提供三类可直接上传的缺陷图像。
